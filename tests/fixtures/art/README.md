# Artwork fixtures

Fixtures are programmatically generated 1×1 metadata-only PNGs, HTML error bodies, empty buffers and ZIP containers. They contain no downloaded artwork.

Local-device cases cover COV precedence over COV2, invalid signatures, duplicate types, missing
Game IDs, traversal/symlink rejection, and deterministic shuffled enumeration.
